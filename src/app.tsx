import { createRoot } from 'react-dom/client';

const root = createRoot(document.body);
root.render(<h2 className="h-screen w-full text-center text-3xl font-bold underline bg-raisinblack text-brightorange">Hello from React!</h2>);